---
- hosts: update
  gather_facts: no
  vars_files:
  - vault/passwords
  - vault/msg_groups.yml
  vars:
    mail_body_file: "/tmp/ansible-mailing-tmtghujm" 
  order: sorted

  tasks:
    - name: run included tasks
      include: task_update_reboot.yml

  post_tasks:
    - name: run included post tasks
      include: task_email.yml
